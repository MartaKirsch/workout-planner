import styled from "styled-components";
import { centeredDiv } from "styles/mixins";

export const AddExercisePageWrapper = styled.div`
  ${centeredDiv}

  height: calc(100vh - var(--logobar-height));
  width: 100%;
`;
